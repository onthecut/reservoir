{
  "name": "onthecut",
  "version": "0.0.0",
  "description": "",
  "main": " ",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run update-notices && npm run serve",
    "serve": "ts-node src/reservoir.ts",
    "import-crt-waterways": "ts-node src/tasks/import-crt-waterways.ts",
    "update-open-data": "ts-node src/tasks/update-open-data.ts",
    "update-notices": "ts-node src/tasks/update-notices.ts"
  },
  "author": "",
  "license": "UNLICENCED",
  "dependencies": {
    "@onthecut/crt-notice-scraper": "^2.0.0",
    "@onthecut/crt-open-data": "^1.0.0",
    "@types/express": "^4.17.8",
    "axios": "^0.20.0",
    "express": "^4.17.1",
    "slonik": "^23.0.0",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.3"
  },
  "devDependencies": {
    "prettier": "^2.1.2"
  }
}
